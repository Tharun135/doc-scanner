# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys

OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify which OpenAI model to use
# Options: gpt-4o-mini (recommended, cost-effective), gpt-3.5-turbo, gpt-4o
OPENAI_MODEL=gpt-4o-mini

# Optional: Adjust AI response creativity (0.0 to 1.0)
# Lower values = more consistent, Higher values = more creative
OPENAI_TEMPERATURE=0.1
